// Curated edits. Separate prefix from /c/ because a collection is not a node in
// the category tree — it cuts across it — and section 5 gives the two different
// item_list_id values.
export default '/@locale/edit/@slug';
