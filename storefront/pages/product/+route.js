// The slug is the translated one, so the Arabic page lives at an Arabic
// address. Section 8A: slugs are stored decoded and percent-encoded exactly
// once at render.
export default '/@locale/products/@slug';
