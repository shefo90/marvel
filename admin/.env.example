# Base URL for the API. Left blank in development: Vite proxies /api to
# localhost:8000 so the browser sees one origin, which is also the production
# topology (marvel.com/admin and marvel.com/api). Set this only when the admin
# moves to its own subdomain.
VITE_API_BASE_URL=
